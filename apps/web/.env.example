# Base URL of the backend API, including the version prefix.
NEXT_PUBLIC_API_URL=http://localhost:4000/api/v1

# Shown in the browser title and on the sign-in screen.
NEXT_PUBLIC_APP_NAME=School ERP Platform
